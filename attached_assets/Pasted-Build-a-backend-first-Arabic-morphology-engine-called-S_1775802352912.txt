Build a backend-first Arabic morphology engine called SmartRoot Arabic v1 inside this project. Keep the existing backend structure, but add a new feature set under /api/smartroot. Create a deterministic rule-based engine, not AI-generated morphology. Support Form I only in v1 for triliteral roots. Implement these modules: normalization.js, rootClassifier.js, patternLibrary.js, lexicon.js, irregularRules.js, conjugationEngine.js, nounEngine.js, explanationEngine.js, and testCases.js. Support these root classes: regular, assimilated, hollow_waw, hollow_ya, defective_waw, defective_ya, and doubled. Add a POST endpoint at /api/smartroot/generate that accepts JSON like {"root":"باع"} and returns normalizedRoot, classification, gloss, confidence, full conjugation for past and present across all 13 pronouns, future with سـ + present, imperative where valid, and nouns including masdar, active participle, and passive participle. Root classification rules: first radical و = assimilated, second radical و = hollow_waw, second radical ي = hollow_ya, third radical و = defective_waw, third radical ي or ى = defective_ya, doubled if final radicals are identical or structurally doubled, else regular. Use lexicon-backed pattern selection for Form I and create a starter lexicon with at least these roots: كتب، فتح، دخل، وعد، وصل، قال، زار، باع، دعا، رمى، حمى، ردّ، مدّ، جرّ. For nouns, prefer lexicon-stored masdar and participles rather than guessing. Add a deterministic explanation engine that explains why the root was classified that way and why the surface forms changed. Add simple confidence values: high if lexicon-backed, medium if mostly rule-generated, low if guessed. Add clear error handling for non-Arabic input and non-triliteral input. Add unit tests for the listed roots checking classification, 3ms past, 3ms present, 2ms imperative, masdar, active participle, and passive participle where applicable. Do not change unrelated project parts. Do not build fancy UI. After implementing, show me the files created, the endpoint path, and 5 sample test outputs for كتب، وعد، باع، دعا، ردّ.